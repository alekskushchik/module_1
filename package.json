{
  "name": "module_1",
  "version": "1.0.0",
  "description": "Module_1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./index.html",
    "build": "rm -rf dist && parcel build ./index.html --public-url /module_1",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^1.12.3"
  }
}
